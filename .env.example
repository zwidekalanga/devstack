# Path to the root workspace containing all service repos.
# Each service is expected at $(CAPITEC_DEVSTACK_WORKSPACE)/<service-name>/
#
# Example (absolute):
#   CAPITEC_DEVSTACK_WORKSPACE=/Users/you/Workspace/capitec-swe-assessment
#
# Example (relative to devstack/):
#   CAPITEC_DEVSTACK_WORKSPACE=..

CAPITEC_DEVSTACK_WORKSPACE=..

# Shared JWT secret for inter-service auth (dev only — override in production)
JWT_SECRET_KEY=dev-only-sentinel-secret-do-not-use-in-prod
